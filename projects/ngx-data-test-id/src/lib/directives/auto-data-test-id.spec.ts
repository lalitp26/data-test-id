import { AutoDataTestId } from './auto-data-test-id';

describe('AutoDataTestId', () => {
  it('should create an instance', () => {
    const directive = new AutoDataTestId();
    expect(directive).toBeTruthy();
  });
});
